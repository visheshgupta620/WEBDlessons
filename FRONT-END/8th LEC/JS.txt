variables :    var , let , const 

data types :   number 
               boolean
               undefined
               object
               function 
               string
 iske array me alg alg data types bhi store ho skte


isme array doubly linked list ke hisaab se bana hota hai - push(peeche add krne ke liye) , pop(peeche se delete) , 
                                                           shift(aage se delete) , unshift(aage value add krne ke liye) 


arr1=[1,2,3,4,5]
                    arr1.slice(starting index,upto index);         arr2=arr1.slice(1,3) --> 2,3 
                 arr1.splice(starting index,count,to add);         arr1.splice(2,1)  -->  1,2,4,5              